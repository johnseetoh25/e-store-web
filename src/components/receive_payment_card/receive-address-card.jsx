import React from 'react'
import style from '@/components/receive_payment_card/cart-info-card.module.css'

export default function ReceiveAddressCard() {
  return (
    <div className={`${style.receive_address_card_layout}`}>
      <h2>Received Address</h2>
    </div>
  )
}
